# Generate a new token for local development here: 
# https://github.com/settings/tokens
# it does not require any custom permissions, the default state allows access 
# to read-only apis
# Once generated, paste it in the space where you see `YOUR_GITHUB_TOKEN` below
GITHUB_ACCESS_TOKEN=YOUR_GITHUB_TOKEN
